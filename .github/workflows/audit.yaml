name: cargo audit

on: 
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  name: Run cargo deny
  cargo-deny:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v3
    - uses: EmbarkStudios/cargo-deny-action@v1
